export default class User {
    constructor(id, firstName, lastName, city, email) {
        this.id = id
        this.firstName = firstName
        this.lastName = lastName
        this.city = city
        this.email = email
    }
}